# Server-only (no NEXT_PUBLIC_ prefix — never shipped to the browser).
# Where next.config.ts's rewrites() proxies /api/* requests to. The browser
# only ever talks to this app's own origin, which is what keeps the session
# cookie first-party instead of a cross-site cookie browsers may block.
API_ORIGIN=http://localhost:4000
